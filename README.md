# Job Portal Flutter App

A Flutter app for posting jobs with Firebase integration.

## Features
- Add/edit job listings  
- Upload PDF CVs (Firebase Storage)  
- Form validation  

## Setup
1. Add `google-services.json` (Android) / `GoogleService-Info.plist` (iOS)  
2. Run:
   ```bash
   flutter pub get
   flutter run
